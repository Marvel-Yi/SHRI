<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.capstone.shri.dao.ApplicationMapper">
    <sql id="selectFields">
        id, user_id, programme_id, signature, management_signature, programme_charge_signature
    </sql>

    <insert id="insertApplication" parameterType="UserApplication" keyProperty="id">
        insert into user_application (user_id, programme_id, signature, management_signature, programme_charge_signature)
        values (#{userId}, #{programmeId}, #{signature}, #{managementSignature}, #{programmeInChargeSignature})
    </insert>

    <select id="hasApplied" resultType="int">
        select count(*) from user_application
        where user_id = #{userId} and programme_id = #{programmeId}
    </select>
</mapper>